# Old way of doing service for an external IP. Removed this to allow traefik to handle ingress
# apiVersion: v1
# kind: Service
# metadata:
#   name: bar100
#   namespace: bar100
# spec:
#   type: LoadBalancer
#   ports:
#     - name: http
#       port: 80
#       targetPort: 80
#       protocol: TCP
#   selector:
#     app: bar100
